**Figure 5** 在论文中位于 **第3.1节 Data Collection（数据收集）** 的结尾部分，是一张由 **(a) 到 (e)** 五个小图组成的汇总图，分别对应前文讲到的**五种不同的数据/计算策略**。下面我按 (a) 到 (e) 的顺序，把每个小图**单拎出来细说**，并在最后给你一张**总览表**，把所有小图串起来。

---

## 一、Figure 5 在讲什么？（总览）

**Figure 5 的标题（我根据正文补全）** 大意是：

> **“面向材料发现与设计的数据库构建、数据清洗、小样本评估及云高性能计算架构的示意图。”**

换句话说，**Figure 5 不是一张图，而是五张图拼在一起，分别展示了第3.1节里讲到的五种不同的“数据/计算策略”**，包括：
- **数据来源与数据库构建**
- **数据清洗与相关性分析**
- **小样本评估方法**
- **云高性能计算（HPC）架构**

---

## 二、逐个拆解 Figure 5 (a) ~ (e)

### Figure 5(a)：ML辅助的Ni-rich正极材料设计合成范式

**来源**：Ref [97]（Wiley, 2024，CC-BY）

**图中展示的内容（根据正文描述还原）：**

> 针对**Ni-rich正极材料（镍-rich阴极材料，用于锂离子电池）**，开发了一种**结合ML的设计合成范式**。其中，**热力学/动力学模拟（thermal/kinetic simulations）** 提供了各种前驱体反应的**边界条件**，同时通过必要的实验构建了**数字图像数据集（digital image dataset）**，作为数据库的资源。

**核心含义（用DS的话说）：**

> **数据来源是多元的**：一部分来自物理模拟（热力学/动力学），提供“物理约束”（边界条件）；另一部分来自真实实验（数字图像），提供“真实标签”。两者结合，构成训练数据库。

**对你DS的意义：**  
材料数据不是“一张表打天下”，而是**模拟数据 + 实验数据 + 专家知识**的融合。这对应DS里的**多源数据融合 (multi-source data fusion)**。

---

### Figure 5(b)：TENG（摩擦纳米发电机）ML模型的数据清洗流程

**来源**：Ref [96]（Elsevier, 2022）

**图中展示的内容（根据正文描述还原）：**

> 为了开发**TENG（摩擦纳米发电机，Triboelectric Nanogenerator）** 的实时电压输出预测模型，首先进行了**数据清洗 (data cleaning)**，剔除**不完整 (incomplete)** 或**不一致 (inconsistent)** 的数据，最终得到一个**包含279个可靠数据点 (reliable data points)** 的精炼数据集 (refined dataset)。

**核心含义（用DS的话说）：**

> 原始数据有缺失值、异常值、格式不统一等问题，经过数据清洗后，得到高质量的数据集（279条）。这是任何DS项目的第一步，材料领域也一样。

**对你DS的意义：**  
你学的 `pandas` 数据清洗（`dropna()`、`fillna()`、剔除异常值）在这里派上了用场。**279条数据**也说明材料领域的数据量通常不大。

---

### Figure 5(c)：TENG数据集中**输入特征与输出变量**之间的**皮尔逊相关系数 (Pearson correlation coefficient)**

**来源**：Ref [96]（Elsevier, 2022）

**图中展示的内容（根据正文描述还原）：**

> 计算了输入特征与输出变量（电压）之间的**皮尔逊相关系数 (Pearson correlation coefficient)**，用热力图（或矩阵图）的形式展示。特别地，**负相关 (negative correlation)** 表明当某些参数增大时，输出电压很可能下降。

**核心含义（用DS的话说）：**

> 这是**相关性分析 (correlation analysis)** ——看每个输入特征与预测目标之间是正相关还是负相关，以及相关程度的强弱。负相关特征告诉你：这个参数越大，目标值越小，值得进一步研究背后的物理机制。

**对你DS的意义：**  
这是你学过的 `df.corr()`。在材料领域，**相关性分析是“数据探索”的第一步**，可以帮助材料科学家锁定哪些参数可能对性能有影响，从而设计更有针对性的实验。

---

### Figure 5(d)：小样本场景下的ML评估方法（重复随机采样子抽样）

**来源**：Ref [98]（Wiley–VCH, 2024）

**图中展示的内容（根据正文描述还原）：**

> 在**可用于模型评估的数据集有限 (limited available dataset)** 的情况下，开发了一种新颖的评估方法：**随机抽取20%作为测试集，其余80%作为训练集，重复100次，最终准确率取100次的平均值。**

**核心含义（用DS的话说）：**

> 这是**重复随机采样子抽样 (repeated random subsampling)**，与标准**k折交叉验证 (k-fold CV)** 不同：每次都是重新随机划分，重复很多次（100次）取平均，以减少单次随机划分带来的方差。

**对你DS的意义：**  
在数据量极小（几十到几百条）时，**评估方法本身也需要定制**——不能照搬标准CV，否则评估结果不稳定。

---

### Figure 5(e)：云高性能计算 (Cloud HPC) 环境下的材料发现工作负载架构

**来源**：Ref [99]（American Chemical Society, 2024）

**图中展示的内容（根据正文描述还原）：**

> 将**ML模型和DFT代码**打包成 **Docker容器镜像 (Docker container images)**。运行时，**工作站虚拟机 (VM)** 将容器镜像拉到 **NetApp Files存储**，并提交作业到 **SLURM作业调度器 (SLURM job scheduler)**，由 **VM规模集 (VM scale sets)** 执行计算。数据和元数据存储在**可搜索的数据库 (searchable database)** 中。该系统成功地从**超过3200万个候选材料 (32 million candidates)** 中快速筛选，预测出约**50万种潜在稳定材料 (half a million potentially stable materials)**。

**核心含义（用DS的话说）：**

> 这是**MLOps/云计算架构**在材料发现中的应用：**Docker容器保证环境一致性 + SLURM调度大规模并行计算 + 弹性VM扩缩容 + 数据库存储结果**，从而实现大规模高通量筛选。

**对你DS的意义：**  
真正的“大规模AI材料发现”不只是一个模型，而是一套**系统工程**——容器化、作业调度、分布式计算、数据存储。这已经超出了“调参”的范畴，进入了**MLOps / 云计算**的领域。

---

## 三、Figure 5 (a) ~ (e) 总览表

| 小图 | 主题 | 材料应用场景 | DS对应概念 | 关键数据/结果 |
|------|------|-------------|-----------|-------------|
| **(a)** | 数据来源与数据库构建 | Ni-rich正极材料设计 | 多源数据融合 (multi-source data fusion) | 热力学/动力学模拟 + 数字图像数据集 |
| **(b)** | 数据清洗流程 | TENG摩擦纳米发电机电压预测 | 数据清洗 (data cleaning) | 279条可靠数据点 |
| **(c)** | 皮尔逊相关系数 | TENG输入-输出相关性分析 | 相关性分析 (correlation analysis) / `df.corr()` | 负相关表明参数增大→电压下降 |
| **(d)** | 小样本模型评估方法 | 钙钛矿太阳能电池钝化材料筛选 | 重复随机采样子抽样 (repeated random subsampling) | 20%测试 + 80%训练，重复100次取平均 |
| **(e)** | 云HPC架构 | 大规模材料发现（3200万候选→50万稳定） | MLOps + 容器化 + 分布式计算 (Docker/SLURM/VM scale sets) | Docker + SLURM + VM scale sets + 搜索数据库 |

---

## 四、Figure 5 想告诉你什么（核心信息）

**Figure 5 不是“一张图”，而是“一套数据策略的图集”**，展示了材料AI系统中**数据层面的五个关键环节**：

1. **数据从哪里来** → (a) 多源融合
2. **数据怎么清洗** → (b) 数据清洗
3. **数据长什么样** → (c) 相关性分析
4. **数据太少怎么办** → (d) 小样本评估
5. **数据太多怎么算** → (e) 云HPC大规模筛选

这五个环节**贯穿了材料AI数据处理的完整链路**：从数据生成、清洗、探索、评估到大规模计算，覆盖了数据科学项目中“数据端”的几乎所有关键步骤。

---

## 五、给你的“Figure 5 阅读理解小练习”

打开论文找到 Figure 5，试着回答以下问题：

1. **(a) 图中画了几个数据来源？它们分别对应什么？**（提示：热力学/动力学模拟 vs 实验）
2. **(c) 图中的色块代表什么？**（提示：红色 vs 蓝色代表什么？）
3. **(e) 图中从“Docker container”到“SLURM”到“VM scale sets”再到“Database”的箭头，代表了什么流程？**

如果每个小图你都能用自己的话说出“这张图在画什么”，那你就真正读懂了 Figure 5。

---

还有不清楚的吗？或者我们直接推进到 **3.2 节 Machine Learning Algorithms（机器学习算法）**？

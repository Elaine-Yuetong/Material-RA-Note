好问题！**Active Learning（主动学习）** 是你在DS课上可能听过但还没深入实践的概念，但在材料领域**极其重要**。我用最通俗的方式给你讲透。

---

## 一、先看传统ML的流程（你熟悉的）

```
有标签数据 (Labeled Data) → 训练模型 → 预测新数据
```

**问题**：你需要**提前准备好所有带标签的数据**。如果标签很难获得（比如需要做实验、做DFT计算，成本很高），那你就卡住了。

---

## 二、Active Learning 的核心思想

> **"让模型主动选择哪些样本最需要被标注，从而用最少的标注成本获得最好的模型性能。"**

**通俗比喻**：

| 场景 | 传统方式 | Active Learning |
|------|---------|-----------------|
| **考试前复习** | 把所有课本从头到尾背一遍 | 先做一套模拟卷，找出不会的题，只复习这些薄弱章节 |
| **看病** | 把所有检查项目全做一遍 | 先问诊，根据症状只做必要的检查 |

**Active Learning = 把标注资源（钱/时间/算力）花在"最有价值"的样本上。**

---

## 三、Active Learning 的工作流程

这是一个**循环迭代**的过程：

```
Step 1: 用少量有标签数据训练一个初始模型（初始模型可能很弱）
        ↓
Step 2: 模型对"无标签数据池 (Unlabeled Pool)"中的每个样本进行预测
        ↓
Step 3: 模型根据某种策略，选出"最有价值"的样本（比如模型最不确定的样本）
        ↓
Step 4: 把这些样本送去标注（做实验/做DFT计算/人工打标签）
        ↓
Step 5: 把新标注的数据加入训练集，重新训练模型（模型变强了）
        ↓
        ↪ 回到 Step 2，重复直到模型足够好或标注预算用完
```

---

## 四、关键的"选样策略"（Query Strategy，查询策略）

模型怎么判断"哪个样本最有价值"？主要有以下几种：

| 策略 | 英文 | 做法 | 直觉 |
|------|------|------|------|
| **不确定性采样** | Uncertainty Sampling | 选模型预测**最不确定**的样本（比如二分类中预测概率接近0.5的） | 模型最"纠结"的样本，信息量最大 |
| **边界采样** | Margin Sampling | 选最高两个类别预测概率**差距最小**的样本 | 类似不确定性采样 |
| **熵采样** | Entropy Sampling | 选预测概率分布**熵最大**的样本（最混乱） | 模型最"迷茫"的样本 |
| **多样性采样** | Diversity Sampling | 选与已有训练集**最不同**的样本 | 覆盖更多样的数据空间 |
| **委员会查询** | Query-by-Committee (QBC) | 多个模型投票，选**分歧最大**的样本 | 多个模型意见不一的样本，信息量大 |

**论文里提到的 FAVAL（特征自适应变分主动学习，Feature-Adaptive Variational Active Learning）** 就是一种**结合了不确定性和多样性**的高级查询策略。

---

## 五、论文中的例子：2D电极材料筛选（Figure 6h）

**场景**：
- 总共有大量候选材料（无标签）
- 但DFT计算很贵，只能计算其中一小部分（15%）
- 问题：**选哪15%去计算？**

**Active Learning 的解决方式**：

```
Step 1: 先用少量已计算的材料训练初始模型
Step 2: 模型对全部候选材料做预测
Step 3: 用FAVAL算法选出"最有价值"的材料（既不确定又具有代表性）
Step 4: 只对这些选出的材料做DFT计算（获得标签）
Step 5: 新数据加入训练集，重新训练模型
Step 6: 重复选样→计算→训练，直到模型足够好
```

**结果**：用 **15%的数据** 就达到了接近全量数据训练的效果。这就是Active Learning的威力！

---

## 六、Active Learning vs 传统ML 对比

| 维度 | 传统ML | Active Learning |
|------|--------|-----------------|
| **数据获取方式** | 一次性准备好全部有标签数据 | **迭代**获取，每次只标注最有价值的样本 |
| **标注成本** | 高（要标注所有样本） | 低（只标注部分样本） |
| **适用场景** | 标签便宜（如MNIST标注） | 标签昂贵（如DFT计算、实验合成） |
| **模型训练** | 训练一次（或CV调参） | **多次迭代**，每次加入新数据 |

---

## 七、一句话总结

> **Active Learning（主动学习） = 让模型主动挑选"最有价值"的样本去标注，用最少的标注成本达到最好的模型效果。**

在材料领域，因为DFT计算和实验合成都非常昂贵，Active Learning几乎是**必修课**。

---

还有其他想深入了解的概念吗？比如**Transfer Learning（迁移学习）** 或 **Autoencoder（自编码器）**？
